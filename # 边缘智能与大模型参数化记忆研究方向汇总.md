# 边缘智能与大模型参数化记忆研究方向汇总

本文档基于对《MLP Memory: A Retriever-Pretrained Memory for Large Language Models》及相关前沿工作（Doc-to-LoRA, Titans, UniMem 等）的调研，结合边缘智能和移动计算的特点，整理了四个具有创新性且实验开销可控的研究方向。

## 方向一：按需激活的稀疏端侧 MLP Memory (Conditional Sparse Edge MLP Memory)

*   **问题背景**：MLP Memory 在处理每个 token 时都运行完整的记忆模块，但在移动设备上，大多数普通语言 token 并不需要外部知识。额外的参数读取和全词表计算会带来高昂的内存带宽和能耗开销。
*   **核心方法**：设计一个轻量的 Conditional Sparse Memory 架构。首先使用门控网络判断当前 token 是否涉及事实性知识或处于高不确定性位置。仅在必要时激活记忆模块，且记忆内部按领域拆分为多个小型 MLP/LoRA 专家，每次仅激活 Top-K 专家。输出端仅拟合检索器预测的 Top-K token 分布。
*   **创新区分度**：有别于全局、始终激活的 MLP Memory，本方向面向现有小模型和端侧严格的资源约束，提出“token 级选择性检索蒸馏”。
*   **实验设计**：
    *   **模型/数据**：Qwen2.5-0.5B/1.5B，总计 20M-100M 参数的记忆模块；NQ, TriviaQA, HotpotQA。
    *   **指标**：准确率、TTFT (Time-to-First-Token)、峰值内存、焦耳/查询。
    *   **预期目标**：以极低的记忆激活率保留大部分准确率收益，实验成本最低。

## 方向二：资源感知且可逆的记忆固化 (Resource-Aware Reversible Memory Consolidation)

*   **问题背景**：端侧新知识不断涌现，但并非所有信息都值得参数化。错误或过期信息一旦固化为参数将难以修改。现有固化机制（如 UniMem）缺乏基于设备资源的决策以及固化后的回滚机制。
*   **核心方法**：建立“情景记忆（向量库） -> 参数记忆（micro-LoRA）”的可逆生命周期。系统综合评估知识访问频率、答案收益、置信度以及固化所需的能耗，仅对高效用知识在设备充电或空闲时进行编译固化。知识过期或冲突时，直接卸载对应模块并回退到检索记忆。
*   **创新区分度**：将端侧设备的资源状态（电量、算力）纳入记忆固化的目标函数，并实现记忆的可逆降级，解决“何时固化、固化多少、何时撤销”的问题。
*   **实验设计**：
    *   **模型/数据**：流式 QA 数据（包含稳定事实、频繁变化知识、错误注入）。
    *   **指标**：准确率、过期知识命中率、遗忘率、存储占用、更新能耗、删除延迟。
    *   **预期目标**：单卡即可验证，能有效展示系统在资源约束下的动态适应能力。

## 方向三：弱网环境下的参数记忆缓存与预取 (Memory Caching & Prefetching in Weak Networks)

*   **问题背景**：移动设备常面临网络波动，无法保存全局知识库，而传统的云边协同 RAG 在弱网下容易产生高延迟。
*   **核心方法**：将 micro-LoRA/MLP 视为可缓存的知识对象。根据用户当前场景、位置和网络预测，提前从边缘服务器下载可能需要的参数记忆模块。内存不足时，基于“预期任务收益/字节/能耗”策略淘汰模块。推理时在本地参数记忆、本地 RAG 和云端 RAG 间动态路由。
*   **创新区分度**：将移动计算中的“缓存与预取”概念引入大模型参数记忆管理，针对弱网移动场景提出解决方案。
*   **实验设计**：
    *   **模型/数据**：基于真实移动网络带宽轨迹和场景切换（医疗、校园、交通等）进行 Trace-driven 仿真。
    *   **指标**：离线可用率、P95 延迟、下载流量、能耗、准确率。
    *   **预期目标**：偏向系统与网络调度，无需大规模边缘集群即可完成仿真验证。

## 方向四：可溯源、可删除的端侧参数记忆 (Traceable & Erasable Edge Parametric Memory)

*   **问题背景**：个人隐私文档参数化后，删除源文件并不能保证模型遗忘该知识；同时模型生成答案时缺乏可解释性和来源证明。
*   **核心方法**：将知识拆分为带独立来源标识的 memory atoms。训练时增加模块隔离和来源对齐约束；推理时不仅输出答案，还预测激活了哪些来源模块。用户请求删除数据时，直接卸载对应原子，并利用检测算法验证知识是否完全遗忘。
*   **创新区分度**：侧重于参数记忆生命周期的安全与合规治理（来源归因、数据删除、泄漏防范），区别于单纯追求效率的方法。
*   **实验设计**：
    *   **模型/数据**：个人档案问答与私密文档 QA 合成数据集。
    *   **指标**：删除前后知识保留差异、错误归因率、成员推断风险。
    *   **预期目标**：创新性高，但严格证明“完全遗忘”有一定难度，适合深入挖掘。